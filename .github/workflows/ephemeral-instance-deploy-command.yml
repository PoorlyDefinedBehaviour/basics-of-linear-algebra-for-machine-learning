name: 'Test'
on:
  issue_comment:
    types: [created]
  pull_request:
      types: [opened, reopened, closed]
jobs:
  printJob:    
    name: Print event
    runs-on: ubuntu-latest
    steps:
      - name: Configure .env file
        run: |
          python -c "import base64; import os; print(os.environ['MY_SECRET'].replace('value', '12345')))"
        shell: bash
        env:
          MY_SECRET : ${{secrets.MY_SECRET}}


 